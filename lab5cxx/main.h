#ifndef MAIN_H
#define MAIN_H

#include <iostream>
#include <iomanip>
#include <fstream>
#include <iterator>
#include <algorithm>
#include <vector>
#include <string>
#include <cctype>
#include <map>

void a(std::vector<std::string>raw_list);
void f(std::vector<std::string>raw_list);
void o(std::vector<std::string>raw_list, int n);
bool has_double_hyphen(std::string word);
//void clean(vector<string>& word_list);
//int update_position(string::iterator it);
//string file_name;


#endif